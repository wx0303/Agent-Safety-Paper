"""Framework adapters live here.

The core package intentionally stays framework-neutral. Adapters translate
runtime-specific callbacks into AgentEvent objects and then apply the shared
GuardrailEngine.
"""

